// Production environment.
// `apiUrl` defaults to a same-origin relative path; set it to the absolute
// API origin (e.g. https://api.iko.app/api) if the API is served elsewhere.
export const environment = {
  production: true,
  apiUrl: '/api',
};
